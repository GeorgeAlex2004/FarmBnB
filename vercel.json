{
  "version": 2,
  "buildCommand": "cd frontend && VITE_BASE_PATH=/frontend/ npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/frontend/$1"
    }
  ]
}
